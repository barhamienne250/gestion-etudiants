spring:
  datasource:
    url: jdbc:mysql://mysql-gestion-etudiants:3306/gestion_etudiants
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080